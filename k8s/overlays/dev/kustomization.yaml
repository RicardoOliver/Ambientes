apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: app-dev

commonLabels:
  environment: dev

configMapGenerator:
  - name: app-config
    literals:
      - APP_ENV=dev
      - LOG_LEVEL=debug
      - FEATURE_FLAGS=all
